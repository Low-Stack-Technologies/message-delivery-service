version: "2"

sql:
  - engine: sqlite
    schema:
      - migrations
    queries:
      - db/queries
    gen:
      go:
        package: db
        out: internal/db/sqlc
        sql_package: database/sql
        emit_json_tags: true
        emit_pointers_for_null_types: true

